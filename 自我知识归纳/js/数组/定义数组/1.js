// 1
// 先对数组进行声明，再对内部元素一个一个赋值
var x = new Array();
x[0] = 1;

// 2
// 先对数组进行声明，且规定长度
// 对数组有长度要求时可以使用此方法
var y = new Array(2);
y[0];

// 3
// 声明时定义
var m = new Array(1,so,true)

// 4
// 声明时定义，但写法更为简洁，也更推荐
var n = [2,ko,false]
