<!-- block 在小程序里有指令 -->
<block wx:for="{{userList}}" wx:key="index">
	<!--block块状元素-->
	<text >{{index}}</text>
</block>
<view class='floatBtn'>
	<button
	 size="mini"
	 bindtap='navigatTo'
	 type="primary"
	 open-type="getUserInfo"
	 bindgetuserinfo="getUserInfo"
	>+
	</button>
</view>

